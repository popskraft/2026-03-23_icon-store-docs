# Icon Store Docs

Public, link-only document portal for the icon store project.

Notes:

- The source project repository remains private.
- This repository is generated automatically from the private project canon.
- Pages are marked with `noindex` and the repository includes a restrictive `robots.txt`.
- Anyone with a direct link can still open a page.
